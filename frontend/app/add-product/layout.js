export const metadata = {
  title: 'Add Product | e-LocalKart Merchant Portal',
  robots: {
    index: false,
    follow: false,
  },
};

export default function AddProductLayout({ children }) {
  return children;
}
